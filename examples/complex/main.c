#include "add.h"
#include "div.h"
#include "mul.h"
#include "sub.h"

int main() {
  int addition = add(1, 1);
  int diffrence = sub(2, 1);
  int multiplication = mul(2, 2);
  int division = div(4, 2);
}
